theme: jekyll-theme-cayman
title: Django and MongoDB connector
description: Djongo translates queries generated by the Django ORM into MongoDB query documents. It is specifically meant to be used with the original Django ORM (instead of a non-rel flavor) and MongoDB
